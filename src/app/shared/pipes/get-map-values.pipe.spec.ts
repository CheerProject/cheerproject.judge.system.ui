import { GetMapValuesPipe } from './get-map-values.pipe';

describe('GetMapValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new GetMapValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
